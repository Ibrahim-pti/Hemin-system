# سیستەمی کارگەی ئاسنگەری هێمن

Laravel 13 + Livewire 3 + Tailwind 4 · تەواو بە کوردی و RTL · MySQL 8

کارگەکە دەرگا، مەحەجەرە، کەپر و مەسعەد دروست دەکات — بۆیە قیاس بە **م² / مەتر / دانە**ـیە.

---

## دەستپێکردن

```bash
php artisan serve          # سێرڤەری گەشەپێدان
npm run dev                # چاودێری فایلەکان (یان: npm run build)
```

**هەژمارەکان**

| ڕۆڵ | ئیمەیل | وشەی نهێنی | دەستڕاگەیشتن |
|---|---|---|---|
| بەڕێوەبەر | `admin@hemin.krd` | `hemin1234` | هەموو شت |
| بەرپرسی کۆگا | `kogha@hemin.krd` | `kogha1234` | تەنها مەخزەن و جەرد — **نرخ نابینێت** |

**فەرمانە تایبەتەکان**

```bash
php artisan hemin:fresh    # سڕینەوەی هەموو مامەڵەکان بۆ دەستپێکردنی کاری ڕاستەقینە
php artisan db:seed        # داتای بنەڕەت (رۆڵ، یەکە، کۆگا، قاسە)
```

---

## یاسا بنەڕەتییەکان — پێش دەستکاری بیانخوێنەوە

**١. باڵانسی مەخزەن هەرگیز هەڵناگیرێت.**
هەمیشە لە `stock_movements` کۆدەکرێتەوە (`Item::stockQty()` / `Item::scopeWithStock()`).
هەرگیز خانەیەکی `qty` زیاد مەکە بۆ `items` — مێژوو و باڵانس دەبنە دوو ڕاستی جیاواز.

**٢. دینار دراوی بنەڕەتە.**
هەر تۆمارێکی دۆلاری `exchange_rate`ـی ئەو ڕۆژە لەگەڵ خۆیدا هەڵدەگرێت
(`App\Models\Concerns\ConvertsCurrency`). بۆیە گۆڕینی نرخی دۆلار **هەرگیز**
راپۆرتی کۆن ناگۆڕێت.

**٣. حەقدی و قاسە هەرگیز جیا نابنەوە.**
هەر پارەیەک لە `PaymentService::record()`ـەوە دەڕوات، کە `Payment` و
`CashTransaction`ـەکەی پێکەوە دروست دەکات لە یەک ترانزاکشندا.

**٤. پێشەکی دووجار ناژمێردرێت.**
`prepaid_amount`ـی وەسڵ خۆکار دەبێتە حەقدییەکی جیا. باڵانسی کڕیار لە
(وەسڵەکان − حەقدییەکان)ـەوە دەردەچێت، نەک لە `prepaid_amount`.

**٥. ناوچەی کات `Asia/Baghdad`ـە** (`config/app.php`).
بەبێ ئەمە مامەڵەی دوای ٩ی شەو دەچێتە ڕۆژی پێشوو.

---

## پێکهاتە

```
app/
  Models/            مۆدێلەکان + Concerns/{ConvertsCurrency, Auditable}
  Services/          StockService · PaymentService · BackupService
  Support/           KurdishNumber (ژمارە بە پیت) · helpers.php (fmt_money…)
  Console/Commands/  FreshStart
resources/views/
  layouts/app        باری سەرەوەی تۆخ + کاتژمێر (بێ سایدبار)
  dashboard          مێنیوی سەرەکی — خانەی ڕەنگاوڕەنگ بە گرووپ
  partials/          icon · stat-tile · calculator
  orders/print       وەسڵی کڕیار — وێنەی دەفتەرە چاپکراوەکە
  payments/print     حەقدی — لەگەڵ بڕی پارە بە پیتی کوردی
```

## دیزاین — یاساکان

- **هیچ shadowێک نییە** — بە زۆر لە `app.css` کوژێنراوەتەوە. جیاکردنەوە تەنها بە بۆردەر.
- ڕەنگی سەرەکی Teal `#0F766E`؛ پاشبنەما تۆنێکی نەرمی مینتی `#EEF4F2`.
- خانەکانی مێنیو بە گرووپ ڕەنگیان هەیە: شین (داتا) · پرتەقاڵی (کڕین و فرۆشتن) ·
  سوور (پارە) · قاوەیی تۆخ (راپۆرت) · قاوەیی (سیستەم).
- هەموو ئایکۆنێک SVGـی ناوخۆییە (`partials/icon`) — فۆنتیش خۆماڵییە،
  بۆیە سیستەم **بێ ئینتەرنێت** کاردەکات.
- ژمارەکان `class="num"` وەردەگرن (LTR + tabular).

## مۆڵەتەکان

بڕوانە `DatabaseSeeder::PERMISSIONS`. بەرپرسی کۆگا تەنها ئەمانەی هەیە:
`view_stock`, `manage_stock`, `manage_items`, `manage_stock_counts`.
بۆ شاردنەوەی نرخ لە ڕوانگەکاندا `@can('view_reports')` بەکاربهێنە.
